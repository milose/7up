// import Vue from 'vue'

// window.Vue = Vue

// const files = require.context('./', true, /\.vue$/i)
// files.keys().map(key =>
//   Vue.component(
//     key
//       .split('/')
//       .pop()
//       .split('.')[0],
//     files(key).default
//   )
// )

// window.store = new Vue({})
